// 22 - Criar uma função para inverter um array

function inverteArray(arr){
    const newArray = arr.slice();
    return newArray.reverse();
}

const inverteArray2 = (arr) => (arr.slice()).reverse();