// 4 - Escreva uma função que retorne a área de um triângulo, a partir dos valores de base e altura fornecidos.

function areaTriangulo(b, h){
    var area = (b*h)/2;
    return area;
}

const areaTriangulo2 = (b, h) => (b*h)/2;
